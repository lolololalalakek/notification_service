# Database Configuration
DB_URL=jdbc:postgresql://localhost:5432/postgres
DB_USERNAME=postgres
DB_PASSWORD=your-database-password

# Email (SMTP) Configuration
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# Basic Auth (dev/test defaults)
APP_SECURITY_BASIC_USERNAME=admin
APP_SECURITY_BASIC_PASSWORD=admin

# SMS Provider Configuration (Eskiz)
SMS_API_TOKEN=your-eskiz-api-token
SMS_SENDER_NUMBER=4546

# Kafka Configuration (optional, defaults in application.yml)
# KAFKA_BOOTSTRAP_SERVERS=localhost:9092
